# Copy this file to .env.local (not committed) and set VITE_CODESPACE_NAME
# to your Codespace name when running in GitHub Codespaces. When unset, the
# app falls back to http://localhost:8000/api for local development.

VITE_CODESPACE_NAME=
